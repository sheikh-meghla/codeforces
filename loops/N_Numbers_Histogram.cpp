#include <bits/stdc++.h>
using namespace std;

int main() {
    char s;
    cin >> s;

    int n;
    cin >> n;

    while (n--) {
        int x;
        cin >> x;

        while (x--) {
            cout << s;
        }
        cout << '\n';
    }

    return 0;
}